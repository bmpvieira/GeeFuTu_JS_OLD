<% include ../head %>

<div class="container pt pb">

    <h2>Experiments</h2>

    <a href="/experiments/add" class="btn btn-default mt mb">Add Experiment</a>

    <% if (experiments.length) { %>
    <table class="table">
        <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Genome</th>

        </tr>
        </thead>
        <tbody>
        <% experiments.forEach(function(experiment){ %>
        <tr>
            <td><a href="/experiments/<%- experiment._id%>"><%- experiment.name %></a></td>
            <td><%- experiment.description %></td>
            <td><%- experiment.genome %></td>
        </tr>
        <% });%>
        </tbody>
    </table>
    <% } %>
</div>

<% include ../foot %>